steps: 
  # Docker Build
  - name: 'gcr.io/cloud-builder/docker'
    args: ['build', '-t', 'gcr.io/enha/subby-server/subby-server-image:latest', '.']

  # Docker push to Google Artifact Registry
  - name: 'gcr.io/cloud-builder/docker'
    args: ['push', 'gcr.io/enhanced-watch-413121/subby-server/subby-server-image:latest']

# Store images in Google Artifact Registry
images:
  - gcr.io/enhanced-watch-413121/subby-server/subby-server-image:latest
